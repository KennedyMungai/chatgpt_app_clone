import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const HeaderDropdown = () => {
	return (
		<View>
			<Text>HeaderDropdown</Text>
		</View>
	)
}

export default HeaderDropdown

const styles = StyleSheet.create({})
